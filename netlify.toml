[build]
  command = "npm run validate:static && npm run generate:sitemap && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[context.production.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
